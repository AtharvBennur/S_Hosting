# Server & App Settings
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Optional Frontend Override (leave blank for local same-origin proxy)
VITE_API_BASE=

# Gemini API Key (Optional)
GEMINI_API_KEY=
